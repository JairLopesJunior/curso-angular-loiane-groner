var minhaVar = 'Minha Variavel';

function minhaFunction(x, y) {
    return x + y;
}

// ES 6 ou ES 2015
let num = 2;
const PI = 3.14;

var numeros = [1, 2, 3];
numeros.map(n => n * 2);

class Matematica {
    soma(x, y) {
        return x + y;
    }
}

