Pipes (usando pipes, parâmetros e pipes aninhados)

O que um pipe faz?
R: O pipe ele transforma um valor e podemos exibir o valor transformado dentro de um template.

Comando para criar um pipi:
> ng g p nomeDoPipe

Os pipes precisam ser adicionados dentro de declarations dentro do module.